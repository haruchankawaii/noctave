{
  "name": "noctave",
  "version": "0.1.0-beta.1",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "dev": "bun scripts/dev.ts",
    "start": "cargo run --release -p noctave",
    "build": "bun run typecheck && cargo build --release --locked",
    "build:ui": "bun scripts/build.ts --outdir target/ui",
    "typecheck": "bun --bun tsc --noEmit",
    "test": "bun test ui",
    "test:smoke": "bun scripts/smoke.ts",
    "check": "bun run typecheck && bun test ui && cargo test --workspace --all-targets --locked",
    "format": "bun --bun prettier --write ui scripts/*.ts package.json tsconfig.json",
    "format:check": "bun --bun prettier --check ui scripts/*.ts package.json tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3",
    "prettier": "^3.6.2"
  }
}
